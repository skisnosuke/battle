github コマンド

【作業開始時】
git checkout master
git fetch
git merge
git checkout shinnosuke
git merge master

【作業終了時】
git add .
git commit -m "message"
git push

【プルリクエスト】
レビューしてもらったら マージ

【現在のブランチを確認】
git branch -a


# github アプリ
## 作業開始時
mainブランチで fetch → pull
自分 ブランチで merge ( 画面上 Branch → Merge into current branch → mainを選択し、青いボタンが Merge main into 自分の名前 になるように) 
自分 ブランチで作業開始

## 作業終了時
左下の欄に メッセージを書き込む → commit(青いボタン)
Push(右上のボタン Push origin)
プルリク(画面上 branch → Create pull request)
コメントを待つ
mainブランチにマージ (Merge pull request → Confirm merge)
